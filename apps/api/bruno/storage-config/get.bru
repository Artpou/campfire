meta {
  name: Get Storage Config
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/storage-config
  body: none
  auth: inherit
}

docs {
  Requires admin role or higher. Returns the SMB config without the password (hasPassword: boolean only).
}
