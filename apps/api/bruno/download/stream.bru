meta {
  name: Stream Download Direct
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/streaming/{{downloadId}}/direct
  body: none
  auth: inherit
}

headers {
  Content-Type:
  Range: bytes=0-
}

docs {
  Requires authenticated session. Byte-range stream of the video file (local or remote). Used by movi-player for seeking.
}
