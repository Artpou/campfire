# Build & run a local image with the same runtime config as docker-compose.yml:
#   docker compose -f docker-compose.build.yml up --build
include:
  - path: docker-compose.yml

services:
  app:
    build: .
    image: seedarr:local
